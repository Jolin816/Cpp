#include <stdio.h>
#include <string.h>
#define maxn 20

int main();
int a[maxn][maxn];

int main() {
    int n, x, y, tot = 0;
    scanf("%d", &n);
    memset(a, 0, sizeof(a));
    tot = a[0][n-1] = 1;
    

}